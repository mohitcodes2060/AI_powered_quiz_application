CREATE TABLE users (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  username VARCHAR(100) NOT NULL UNIQUE,
  password VARCHAR(255) NOT NULL,
  email VARCHAR(255) NOT NULL,
  role VARCHAR(50) NOT NULL
);

CREATE TABLE quizzes (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  description VARCHAR(1000) NOT NULL,
  created_by VARCHAR(100) NOT NULL,
  is_active BOOLEAN NOT NULL DEFAULT TRUE
);

CREATE TABLE questions (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  quiz_id BIGINT NOT NULL,
  question_text VARCHAR(1000) NOT NULL,
  choice_a VARCHAR(255) NOT NULL,
  choice_b VARCHAR(255) NOT NULL,
  choice_c VARCHAR(255) NOT NULL,
  choice_d VARCHAR(255) NOT NULL,
  correct_answer VARCHAR(255) NOT NULL,
  CONSTRAINT fk_question_quiz FOREIGN KEY (quiz_id) REFERENCES quizzes(id) ON DELETE CASCADE
);

CREATE TABLE quiz_results (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id BIGINT NOT NULL,
  quiz_id BIGINT NOT NULL,
  score INT NOT NULL,
  total_questions INT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT fk_result_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  CONSTRAINT fk_result_quiz FOREIGN KEY (quiz_id) REFERENCES quizzes(id) ON DELETE CASCADE
);

INSERT INTO users (username, password, email, role) VALUES
 ('admin', 'admin123', 'admin@quiz.com', 'admin'),
 ('user1', 'user123', 'user1@quiz.com', 'user');

INSERT INTO quizzes (title, description, created_by, is_active) VALUES
 ('General Knowledge Quiz', 'Test your knowledge with these general questions', 'admin', TRUE);

INSERT INTO questions (quiz_id, question_text, choice_a, choice_b, choice_c, choice_d, correct_answer) VALUES
 (1, 'What is the capital of France?', 'London', 'Berlin', 'Paris', 'Madrid', 'Paris'),
 (1, 'Which planet is known as the Red Planet?', 'Earth', 'Mars', 'Jupiter', 'Venus', 'Mars'),
 (1, 'What is 2 + 2 * 4?', '16', '10', '8', '6', '10');

INSERT INTO quizzes (title, description, created_by, is_active) VALUES
 ('General Knowledge - Basics', 'Six simple questions for demo', 'mohit', TRUE);

INSERT INTO questions (quiz_id, question_text, choice_a, choice_b, choice_c, choice_d, correct_answer) VALUES
 ( (SELECT id FROM quizzes WHERE title = 'General Knowledge - Basics'),
   'What is the capital of Australia?', 'Sydney', 'Melbourne', 'Canberra', 'Perth', 'Canberra'),
 ( (SELECT id FROM quizzes WHERE title = 'General Knowledge - Basics'),
   'Which is the largest planet in the solar system?', 'Mercury', 'Earth', 'Jupiter', 'Mars', 'Jupiter'),
 ( (SELECT id FROM quizzes WHERE title = 'General Knowledge - Basics'),
   '9 + 6 * 2 = ?', '15', '21', '30', '12', '21'),
 ( (SELECT id FROM quizzes WHERE title = 'General Knowledge - Basics'),
   'Which file extension is used for Java source code?', '.class', '.jar', '.java', '.exe', '.java'),
 ( (SELECT id FROM quizzes WHERE title = 'General Knowledge - Basics'),
   'CPU stands for ____.', 'Central Program Unit', 'Central Processing Unit', 'Computer Personal Unit', 'Control Processing Utility', 'Central Processing Unit'),
 ( (SELECT id FROM quizzes WHERE title = 'General Knowledge - Basics'),
   'Which language was created by Guido van Rossum?', 'Java', 'C++', 'Python', 'Ruby', 'Python');
